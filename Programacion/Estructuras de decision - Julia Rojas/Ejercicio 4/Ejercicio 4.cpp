#include <stdio.h>

int main()
{
	int nivel_acceso;

	printf("Ingrese su nivel de acceso: \n");
	scanf_s("%d", &nivel_acceso);

	if (nivel_acceso == 1);
	{
		printf("Se ha permitido el acceso a la configuracion del router.");
	}
}